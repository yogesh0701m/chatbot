# Nhost Configuration
VITE_NHOST_SUBDOMAIN=your-nhost-subdomain
VITE_NHOST_REGION=us-east-1

# Optional: Direct GraphQL endpoint (if not using Nhost client)
VITE_GRAPHQL_ENDPOINT=https://your-nhost-subdomain.nhost.run/v1/graphql